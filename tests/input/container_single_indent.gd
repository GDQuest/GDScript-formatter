func function_with_long_parameters(first_parameter_with_a_long_name: String, second_parameter_with_a_long_name: String, third_parameter_with_a_long_name: String) -> void:
	print_debug("Trying to transition to state " + possible_transitions[event].name + " from state " + state.name + " but the transition does not exist.")
	print_debug("Could not load resource " + resource_path + " for node " + node.name + " because " + error_message + ". Verify that the path exists and the resource type is supported.")
	set_total(base_cost + upgrade_cost + shipping_cost + regional_tax + optional_insurance + handling_fee + service_charge + discount_adjustment)
	set_collision_mask(LAYER_PLAYER | LAYER_ENEMY | LAYER_PROJECTILE | LAYER_WORLD | LAYER_INTERACTABLE | LAYER_DESTRUCTIBLE | LAYER_WATER)
	assert(player.is_alive() and player.has_input_focus() and not player.is_stunned() and target.is_valid() and target.is_visible_in_tree() and navigation_agent.is_navigation_finished())
	var result = first_condition_with_a_moderately_long_name and second_condition_with_a_moderately_long_name
	set_total(first_balanced_value_with_a_consistent_width + second_balanced_value_with_a_consistent_width + third_balanced_value_with_a_consistent_width + fourth_balanced_value_with_a_consistent_width + fifth_balanced_value_with_a_consistent_width + sixth_balanced_value_with_a_consistent_width)
