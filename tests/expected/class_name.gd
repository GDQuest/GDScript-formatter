# class_name with extends at top level
# Should each be on their own line. TODO: add variants with annotations.
class_name Player extends CharacterBody2D

class_name UI extends Control

class_name MyNode extends "res://path.gd"

@abstract class_name MyNode extends Node
